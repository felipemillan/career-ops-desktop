/**
 * PipelineTab — STUB. Phase 2 agent fills this in. Owns this file + its own components/hooks.
 * Use ipc.ts + store.ts + parsers only. No direct invoke/node:fs.
 */
export function PipelineTab() {
  return (
    <div className="text-sm text-gray-500 dark:text-gray-400 p-8">
      Pipeline — coming in Phase 2.
    </div>
  );
}
